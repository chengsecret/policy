<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="site.koisecret.auth.mapper.CollectionMapper">
    <insert id="addCollection" parameterType="site.koisecret.auth.entity.Collection">
        insert into policy.collection (uid,pid)
        values(#{uid}, #{pid})
    </insert>

</mapper>